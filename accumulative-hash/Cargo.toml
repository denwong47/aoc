[package]
name = "accumulative-hash"
version = "0.1.0"
edition = "2024"

[features]
default = ["set"]
atomic = []
set = ["nohash"]
nohash = ["dep:nohash"]

[dependencies]
nohash = { version = "0.2.0", optional = true }
num-traits = "0.2.19"
